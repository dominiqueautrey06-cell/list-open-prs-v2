
name: List Open PRs

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  list-prs:
    runs-on: ubuntu-latest
    steps:
      - name: List open PRs
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr list --state open

